{
  "name": "classroom-seating",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Zero-build, privacy-first classroom seat planner (pure static site)",
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node tests/_node.mjs",
    "lint": "eslint js/ packages/ tests/*.js tests/*.mjs scripts/"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.0",
    "eslint": "^9.30.0",
    "globals": "^16.0.0",
    "playwright-core": "^1.62.1"
  }
}
